org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G
android.useAndroidX=true
android.enableJetifier=true

# Java 25 (bundled with the newest Android Studio) cannot run Gradle 8.x;
# use the JDK 21 bundled with the older Android Studio install for builds.
org.gradle.java.home=/snap/android-studio/236/jbr
